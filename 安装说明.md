# GPT6省token工具v1.0

显示名：GPT6省token工具v1.0。调用标识：`$gpt6-token-saver-v1-0`。
Skill 本体只有 SKILL.md 和 agents/openai.yaml；不需要插件或 MCP。

## 安装到另一台电脑

复制整个文件夹并进入该文件夹。需要已安装 Codex、可用的 Astra/Sol 和 Python 3.11+（若 python3 指向旧版，请换成 python3.11 或更新版本的实际路径）。安装脚本不安装软件、不登录、不修改权限配置。

```bash
python3 install.py --astra medium --sol medium --context-config stdin <<'TOML'
[features.context_management]
experimental_mode = true
TOML
```

只需修改 `--astra` 和 `--sol` 后面的值，各自可选 `low`、`medium`、`high`、`xhigh`、`max`、`ultra`，实际可用级别仍取决于目标电脑版本与账号。默认都是 medium；不会自动提高强度。

安装脚本会：

- 将 Skill 放到 `~/.agents/skills/gpt6-token-saver-v1-0/`。
- 合并 `${CODEX_HOME:-~/.codex}/config.toml`，将新任务默认模型设为 Astra、子代理默认模型设为 Sol，并分别写入选定强度。
- 启用子代理，以及上述 context_management 配置；修改已有配置前生成仅本人可读的备份。
- 保留无关设置；同名 Skill 内容不同或配置写法无法安全合并时停止。重复运行同一参数不会重复添加配置。

`config.example.toml` 展示默认安装后的相关配置。安装会改变用户级默认模型和子代理默认模型；已有任务、项目配置或界面选择可能覆盖默认值。

## 使用

重新打开 Codex；主任务选择 **GPT-6 Astra** 和期望的推理强度。输入：

```text
使用 $gpt6-token-saver-v1-0，Astra=medium，Sol=medium。任务：……
```

聊天中的文字不能修改已运行主任务的模型或推理强度；Astra 的实际强度须同步在界面选择。Sol 的强度在创建子代理时显式传入。也可以从项目目录启动新任务：

```bash
codex -m gpt-6-astra -c 'model_reasoning_effort="medium"' '$gpt6-token-saver-v1-0 使用已配置的 Sol 推理强度。任务：……'
```

模型限制适用于此 Skill 工作流，属于指令约束，并非 Codex 全局硬白名单。执行委派受当前工具和更高优先级规则约束；不满足条件时停止执行，不以 Astra 或其他模型代替 Sol。不保证每个任务的总 token 或费用必然下降。

## 已核对与验证（2026-09-11）

- 官方 [Skill 结构与发现路径](https://learn.chatgpt.com/docs/build-skills)：YAML frontmatter，SKILL.md，用户目录 ~/.agents/skills。
- 官方 [子代理说明](https://learn.chatgpt.com/docs/agent-configuration/subagents) 与 [配置参考](https://learn.chatgpt.com/docs/config-file/config-reference)：default_subagent_model、default_subagent_reasoning_effort 和 features.context_management.experimental_mode。后者要求 ChatGPT 登录及 Plus、Pro 或 Pro Lite。
- 官方 [插件打包语法](https://developers.openai.com/plugins/build/plugins)：新格式为根目录 plugin.json；.codex-plugin/plugin.json 仍是兼容格式。本交付采用直接安装 Skill，省去插件层。
- 本机 Codex CLI 0.153.4 模型目录确认两个精确模型 ID 及列出的推理强度；安装生成的配置通过隔离环境的 app-server --strict-config 加载。
- 已验证 YAML、配置合并、不同强度选择、重复安装、备份、无关设置保留和同名修改文件保护。未进行真实模型请求或跨电脑端到端调用。内置 quick_validate.py 缺少 PyYAML，已用 Ruby YAML 解析和命名检查替代；未安装额外依赖。

这些文件是交付包；制作时没有修改当前电脑的用户级 Codex 配置。
